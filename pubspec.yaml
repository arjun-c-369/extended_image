name: extended_image
description: Official extension image, support placeholder(loading)/ failed state, cache network, zoom/pan, photo view, slide out page, editor(crop,rotate,flip), painting etc.
version: 4.2.1
homepage: https://github.com/fluttercandies/extended_image

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  #extended_image_library: ^3.1.0
  extended_image_library:
    git:
      url: https://github.com/arjun-c-369/extended_image_library.git
      ref: 90e79d694f4622602e8f416e97a6c7f4e27a92ba
  flutter:
    sdk: flutter
  meta: ^1.1.8

